

// -------------------------------------------------// -------------------------------------------------

const first = 'Jaan'
const last = 'pakhi'

const name = `My girl Friend Name is ${first} ${last}`
console.log(name);